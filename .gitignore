# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# NOTE: the v4 Söhne + Martina Plantijn woff2 in /public/fonts are Klim TRIAL
# fonts, now committed + deployed per Dan's call (2026-06-26) while the full
# licence is finalised. Swap these for the full-licence files once it lands.

# typescript
*.tsbuildinfo
next-env.d.ts
